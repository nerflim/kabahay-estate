import React from 'react';
import SearchBar from '../../SearchBar';

const Search = () => {
	return (
		<React.Fragment>
			<div>
				<SearchBar />
			</div>
		</React.Fragment>
	);
};

export default Search;
